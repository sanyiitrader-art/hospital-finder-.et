package com.hospitalfinder.app.theme

/**
 * The three theme choices exposed in Settings. SYSTEM_DEFAULT means
 * "follow the Android device's current light/dark appearance" — it is
 * not a fourth manually-defined color scheme.
 */
enum class ThemeMode {
    LIGHT,
    DARK,
    SYSTEM_DEFAULT
}