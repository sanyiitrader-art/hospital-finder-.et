<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingHorizontal="@dimen/auth_horizontal_padding">

    <ImageButton
        android:id="@+id/btn_close"
        android:layout_width="@dimen/auth_close_button_size"
        android:layout_height="@dimen/auth_close_button_size"
        android:layout_marginTop="8dp"
        android:padding="12dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_close"
        android:contentDescription="@string/close_login"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/txt_app_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="72dp"
        android:text="@string/app_name_login"
        android:textColor="@color/teal_accent_dark"
        android:textSize="26sp"
        android:textStyle="bold"
        android:letterSpacing="0.02"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <EditText
        android:id="@+id/input_username"
        android:layout_width="0dp"
        android:layout_height="@dimen/auth_input_height"
        android:layout_marginTop="56dp"
        android:background="@drawable/bg_auth_input"
        android:paddingHorizontal="@dimen/auth_input_padding_horizontal"
        android:hint="@string/hint_username"
        android:textColor="@color/text_primary"
        android:textColorHint="@color/text_secondary"
        android:textSize="@dimen/text_size_body"
        android:inputType="text"
        android:maxLines="1"
        android:imeOptions="actionNext"
        app:layout_constraintTop_toBottomOf="@id/txt_app_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/txt_username_error"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/auth_error_spacing_top"
        android:text="@string/error_missing_username"
        android:textColor="@color/error_text"
        android:textSize="@dimen/auth_error_text_size"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/input_username"
        app:layout_constraintStart_toStartOf="@id/input_username"
        app:layout_constraintEnd_toEndOf="@id/input_username" />

    <EditText
        android:id="@+id/input_phone"
        android:layout_width="0dp"
        android:layout_height="@dimen/auth_input_height"
        android:layout_marginTop="@dimen/auth_field_spacing"
        android:background="@drawable/bg_auth_input"
        android:paddingHorizontal="@dimen/auth_input_padding_horizontal"
        android:hint="@string/hint_phone_number"
        android:textColor="@color/text_primary"
        android:textColorHint="@color/text_secondary"
        android:textSize="@dimen/text_size_body"
        android:inputType="phone"
        android:maxLines="1"
        android:imeOptions="actionDone"
        app:layout_constraintTop_toBottomOf="@id/txt_username_error"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/txt_phone_error"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/auth_error_spacing_top"
        android:text="@string/error_missing_phone"
        android:textColor="@color/error_text"
        android:textSize="@dimen/auth_error_text_size"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/input_phone"
        app:layout_constraintStart_toStartOf="@id/input_phone"
        app:layout_constraintEnd_toEndOf="@id/input_phone" />

    <TextView
        android:id="@+id/txt_or"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/auth_or_spacing_vertical"
        android:text="@string/label_or"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/auth_or_text_size"
        app:layout_constraintTop_toBottomOf="@id/txt_phone_error"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <LinearLayout
        android:id="@+id/btn_google"
        android:layout_width="0dp"
        android:layout_height="@dimen/auth_button_height"
        android:layout_marginTop="@dimen/auth_or_spacing_vertical"
        android:orientation="horizontal"
        android:gravity="center"
        android:background="@drawable/bg_auth_input"
        android:foreground="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintTop_toBottomOf="@id/txt_or"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="@dimen/auth_google_icon_size"
            android:layout_height="@dimen/auth_google_icon_size"
            android:src="@drawable/ic_google"
            android:contentDescription="@string/google_icon_desc" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="@string/btn_continue_google"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_size_body"
            android:textStyle="bold"
            android:gravity="center" />
    </LinearLayout>

    <TextView
        android:id="@+id/btn_continue"
        android:layout_width="0dp"
        android:layout_height="@dimen/auth_button_height"
        android:layout_marginTop="40dp"
        android:layout_marginBottom="32dp"
        android:background="@drawable/bg_button_filled"
        android:foreground="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:text="@string/btn_continue"
        android:textColor="@android:color/white"
        android:textSize="@dimen/text_size_body"
        android:textStyle="bold"
        android:letterSpacing="0.05"
        app:layout_constraintTop_toBottomOf="@id/btn_google"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>